export default function ToggleInfo() {
  // const showInfo = $(".toggle-info");

  // if (showInfo && showInfo.length > 0) {
  //   showInfo.on("click", function () {
  //     $(this).toggleClass("active");
  //     $(this).next().toggleClass("active");
  //   });
  // }
}
